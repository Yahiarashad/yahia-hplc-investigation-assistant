# Yahia QC Instrument Lifecycle & Investigation Intelligence

Standalone Streamlit app directory. Deploy `instrument_app/app.py` as the Community Cloud entrypoint so the root `pages/` directory from the HPLC/Decision Gap app is not auto-discovered.
