name: Apply Instrument 360 health score fix
on:
  push:
    branches: [instrument-lifecycle-standalone]
    paths: [.github/workflows/apply_i360_health_score.yml]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: instrument-lifecycle-standalone
          fetch-depth: 0
      - run: python tools/patch_i360_health_score.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add instrument_supabase_app.py
          git commit -m "Make Instrument 360 health score visible on mobile"
          git push origin HEAD:instrument-lifecycle-standalone
